# Terraform state — NUNCA versionar (contém senhas!)
*.tfstate
*.tfstate.backup
.terraform/

# .terraform.lock.hcl DEVE ser versionado (boa prática oficial HashiCorp)

# Variáveis com dados sensíveis
terraform.tfvars

# Chaves privadas SSH
*.pem
*.key

# Arquivos de evidência locais (opcionais)
evidencia-plan.txt
